import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function ConfigsPage() {
  return (
    <Card>
      <Title level={2}>System Configuration</Title>
      <p>Configuration management page - system settings</p>
    </Card>
  );
}
