import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function ImportPage() {
  return (
    <Card>
      <Title level={2}>Import Debtors</Title>
      <p>Import functionality placeholder - file upload for batch debtor import</p>
    </Card>
  );
}
