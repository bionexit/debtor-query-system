import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function ImportResultPage() {
  return (
    <Card>
      <Title level={2}>Import Result</Title>
      <p>Import result page - displays results of batch import operation</p>
    </Card>
  );
}
