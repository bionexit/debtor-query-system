import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function PartnersPage() {
  return (
    <Card>
      <Title level={2}>Partners</Title>
      <p>Partner management page - API partner configuration</p>
    </Card>
  );
}
